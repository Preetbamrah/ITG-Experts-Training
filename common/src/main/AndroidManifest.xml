<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.common">

    <application
        android:allowBackup="true"
        android:icon="@drawable/itgback"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.AppActivity" />
        <activity android:name=".activities.FacultyProfile" />
        <activity android:name=".activities.RegisteredStudentActivity" />
        <activity android:name=".activities.ViewStatusActivity" />
        <activity android:name=".activities.StatusUpdate" />
        <activity android:name=".activities.Dashboard" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginPage" />
        <activity android:name=".activities.RegisterPage" />
        <activity android:name=".activities.Faculty_status" />
        <activity android:name=".activities.ViewStatusList"></activity>
    </application>

</manifest>